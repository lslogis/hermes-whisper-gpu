name: whisper-gpu
version: 1.2.0
description: "GPU speech-to-text for Hermes' local Whisper. Windows: restores CUDA 12 cuBLAS/cuDNN DLLs (pinned NVIDIA wheels, SHA-256 verified) at load after updates, no restart needed. macOS: transcribes on a local MLX server with CPU fallback."
author: lslogis
